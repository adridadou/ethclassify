var should = chai.should();

describe('EthFileReader', () => {
  const progressbar = {};
  const ethCrypto = {};
  const documentManager = {};
  const account = {};
  const ipfs = {};
  const fileReader = new EthFileReader(progressbar, ethCrypto, documentManager,account, ipfs);
  describe('onStart', () =>  {
    //TODO: write unit tests
  });
  describe('onFinish', () =>  {
    //TODO: write unit tests
  });
  describe('onRead', () =>  {
    //TODO: write unit tests
  });  
  describe('processFile', () =>  {
    //TODO: write unit tests
  });
  describe('encryptFile', () =>  {
    //TODO: write unit tests
  });  
  describe('publishFile', () =>  {
    //TODO: write unit tests
  });  
});